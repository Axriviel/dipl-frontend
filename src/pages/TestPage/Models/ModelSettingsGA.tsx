export interface IModelSettingsGA {
    generations: number;          
    populationSize: number;       
    numParents: number;           
    mutationRate: number;         
    selectionMethod: string;      
}