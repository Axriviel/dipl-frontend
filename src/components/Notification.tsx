interface Props {
    message: string,
}

//used to diplay any notification for user (e.g. succesfully logged in)
export const Notification = (props: Props) => {
    return (
        <div>

        </div>
    );
};